# Assembly / edit map

Use the narration in `script.md` as the master timeline. Exact timings can flex with the recorded read, but preserve this order.

1. **Hook (0:00–0:20)** — three-concept title card; quick cuts; no code yet.
2. **KeyPair anatomy (0:20–0:55)** — source capture with two highlight passes: secret signing key, public verifying key.
3. **Generation (0:55–1:25)** — source capture; add simple arrows showing seed → signing key → verifying key.
4. **Public formats (1:25–1:55)** — source capture, then clean public-data text card.
5. **RTC address derivation (1:55–2:35)** — slow enough for viewers to read the exact hash/prefix formula; this is the primary technical beat.
6. **Tests (2:35–2:55)** — punch in on format/determinism assertions; avoid scrolling quickly.
7. **Sign vs verify (2:55–3:30)** — split-screen source: signing method left, verification method right; visually label PRIVATE vs PUBLIC roles.
8. **Deterministic demo (3:30–4:00)** — terminal capture with public fixed vector and derived address. Hold final address for at least 3 seconds.
9. **Security boundary (4:00–4:30)** — full-screen safe/secret checklist. This is the retention/safety beat.
10. **Close (4:30–end)** — source file + repo URL + author attribution.

## Audio / caption notes

- Burn in captions for every line of narration.
- Highlight code terms exactly as written: `SigningKey`, `VerifyingKey`, `SHA-256`, `RTC`, `64 bytes`.
- Do not add music or effects that imply financial performance or guaranteed security.
- If an editor trims for length, preserve the address-derivation formula and the private/public-key boundary before secondary details.
